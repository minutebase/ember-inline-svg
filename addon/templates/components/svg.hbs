<svg
  class={{class}}
  aria-labelledby="{{labelledBy}}"
  role="{{role}}"
  viewBox="{{viewBox}}"
  width={{width}}
>
  {{#if title}}
    <title class="title">{{title}}</title>
  {{/if}}
  {{#if desc}}
    <desc class="desc">{{desc}}</desc>
  {{/if}}
  {{svg}}
  {{yield}}
</svg>
