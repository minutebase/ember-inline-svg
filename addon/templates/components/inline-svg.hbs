<svg class={{class}} aria-labelledby="{{labelledBy}}" role="{{role}}">
  {{#if title}}
    <title class="title">{{title}}</title>
  {{/if}}
  {{#if desc}}
    <desc class="desc">{{desc}}</desc>
  {{/if}}
  {{svg}}
  {{yield}}
</svg>
